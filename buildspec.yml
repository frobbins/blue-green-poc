version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm run install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - npm run test
artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: build
  name: wgt-guru
