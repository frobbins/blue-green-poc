version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm ci
  build:
    commands:
      - npm run build
  test:
    commands:
      - npm run test
artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: build
  name: wgt-guru
